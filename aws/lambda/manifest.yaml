name: aws-lambda
version: 0.0.1
type: service
required_identities:
  - aws:iam:role
capabilities:
  - schedules
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/plugins//aws/lambda/module
runtime:
  go_module: github.com/nitrictech/plugins/aws/lambda

inputs:

outputs:
