name: aws-cloudfront
type: entrypoint
icon: ./icon.svg
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/plugins//aws/cloudfront/module

inputs: {}

outputs: {}