name: aws-iam-role
type: identity
identity_type: aws:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/plugins//aws/iamrole/module

inputs:

outputs:
