name: aws-s3-bucket
type: storage
description: "Provisions S3 buckets with encryption, versioning, and lifecycle policies"
icon: ./icon.svg
required_identities:
  - aws:iam:role
deployment:
  terraform: ./module
runtime:
  go_module: github.com/nitrictech/plugins/aws/s3

inputs:
  tags:
    type: map(string)
    description: "Tags to apply to the S3 bucket"

outputs: {}
