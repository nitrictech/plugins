name: aws-s3-bucket
version: 0.0.1
type: storage
required_identities:
  - aws:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/plugins//aws/s3/module
runtime:
  go_module: github.com/nitrictech/plguins/aws/s3

inputs:

outputs:
