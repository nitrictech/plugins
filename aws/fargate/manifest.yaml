name: aws-fargate
type: service
required_identities:
  - aws:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/plugins//aws/fargate/module
runtime:
  go_module: github.com/nitrictech/plugins/aws/fargate

inputs:

outputs:
